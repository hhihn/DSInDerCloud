minikube start
eval $(minikube docker-env)
# build
docker build -t kedro_pipeline:latest kedro_pipeline/churn-prediction

# test
docker run kedro_pipeline:latest

# mit pod verbinden

kubectl exec -it model-service-6d8f9c7c8d-abcde -- sh